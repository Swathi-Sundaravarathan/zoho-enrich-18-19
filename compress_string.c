#include<stdio.h>
#include <stdlib.h>

int main()
{
    char s[100];
    int i,j,count,l;
    scanf("%s",s);
    l=strlen(s);
    for(i=0;i<l;i++)
    {
        count=1;
        while(strcmp(s[i],s[i+1]==0)
        {
            count++;
            i++;
        }
        printf("%c%d",a[i],c);
    }
}
